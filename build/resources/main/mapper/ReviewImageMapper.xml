<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jsl26tp.jsl26tp.mapper.ReviewImageMapper">

    <!-- 리뷰의 이미지 목록 -->
    <select id="findByReviewId" resultType="ReviewImage">
        SELECT * FROM review_images WHERE review_id = #{reviewId}
    </select>

    <!-- 이미지 등록 -->
    <insert id="insertImage" parameterType="ReviewImage">
        INSERT INTO review_images (review_id, image_url)
        VALUES (#{reviewId}, #{imageUrl})
    </insert>

    <!-- 이미지 삭제 (리뷰의 모든 이미지) -->
    <delete id="deleteByReviewId">
        DELETE FROM review_images WHERE review_id = #{reviewId}
    </delete>

</mapper>
